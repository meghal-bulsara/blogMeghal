<div id="page_wrapper">
  <h1>New Page</h1>
  <%= form_for @post do |f| %>
  <% if @post.errors.any? %>l
  <% end %>
  <div id="errors">
    <h2> <%= pluralize(@post.errors.count, "error") %>Prevented this tpost from saving</h2>
    <ul>
      <%= @post.errors.full_messages.each do |msg| %>
      <li> <%= msg %></li>
      <% end %>
    </ul>
  </div>
  <p>
    <%= f.label :title %><br/>
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :body %><br/>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
  <% end %>
</div>
